# Core Flask application dependencies (minimal - only for template rendering)
Flask==3.0.3
werkzeug==3.0.4

# FastAPI and ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-jose[cryptography]>=3.3.0  # JWT authentication
python-multipart>=0.0.12

# Security and authentication
bcrypt==4.2.0
cryptography==43.0.1
passlib
rsyslog-logger==1.0.5
# HTTP client and utilities
requests>=2.32.3
python-dotenv>=1.0.1
psutil>=6.0.0

# GUI dependencies (for shutdown daemon system tray)
pystray>=0.19.5
pillow>=10.4.0

# Optional: Enhanced CLI argument parsing (already in Python stdlib as argparse)
# argparse - built-in module

# Optional: Enhanced JSON handling (already in Python stdlib)
# json - built-in module

# Requirements for packaging and distribution
pyinstaller